# ============================================
# Backend 環境変数設定
# ============================================
# セットアップ: cp .env.example .env
#
# 開発は Docker Compose 環境で行います
# DATABASE_URL のホスト名は db:3306（Docker内部ネットワーク）

# ============================================
# アプリ設定値（非機密 Config）
# ============================================
APP_ENV=development
APP_LOG_LEVEL=debug
BACKEND_PORT=3000

# ============================================
# データベース設定（機密情報）
# ============================================
# MySQL 接続文字列（Docker Compose 環境用）
# 形式: mysql://user:password@host:port/database
# 開発環境: Prisma マイグレーションに shadow DB 権限が必要なため root を使用
DATABASE_URL="mysql://root:rootpassword@db:3306/chat_app"

# ============================================
# JWT 認証設定（機密情報）
# ============================================
# JWT 署名用シークレットキー（本番環境では必ず強力なランダム文字列を使用）
JWT_SECRET="your-super-secret-key-minimum-32-characters"
# JWT トークンの有効期限
JWT_EXPIRES_IN="1h"
