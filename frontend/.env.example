# =============================================================================
# Frontend Environment Variables
# =============================================================================
#
# セットアップ: cp .env.example .env
#
# 開発は Docker Compose 環境で行います
# API/WebSocket URL は Docker 内部ネットワーク名（backend:3000）を使用
#
# =============================================================================

# Application Environment
# development | staging | production
APP_ENV=development

# API Configuration
# Docker ネットワーク内のサービス名で接続
NEXT_PUBLIC_API_BASE_URL=http://backend:3000

# WebSocket 接続先 URL
NEXT_PUBLIC_WS_URL=ws://backend:3000

# Application Version
NEXT_PUBLIC_APP_VERSION=local
